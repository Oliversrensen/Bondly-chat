services:
  - type: web
    name: bondly-websocket
    env: node
    plan: free
    buildCommand: npm install && prisma generate && prisma migrate deploy
    startCommand: npm run ws
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /
